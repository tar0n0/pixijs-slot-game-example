/*!
 * @pixi/lights - v4.0.0
 * Compiled Thu, 22 Dec 2022 00:00:35 UTC
 *
 * @pixi/lights is licensed under the MIT License.
 * http://www.opensource.org/licenses/mit-license
 * 
 * Copyright 2017-2021, Ivan Popelyshev, All Rights Reserved
 */
import { Matrix, Texture } from '@pixi/core';
import { MeshMaterial } from '@pixi/mesh';
import { vert } from '../shared.mjs';

class LightShader extends MeshMaterial {
  constructor(options) {
    const uniforms = {
      translationMatrix: Matrix.IDENTITY.toArray(true),
      uNormalSampler: Texture.WHITE,
      uViewSize: new Float32Array(2),
      uViewPixels: new Float32Array(2),
      uLightFalloff: new Float32Array([0, 0, 0]),
      uLightHeight: 0.075,
      uBrightness: 1,
      uUseViewportQuad: true
    };
    Object.assign(uniforms, options?.uniforms);
    super(Texture.WHITE, { ...options, uniforms });
  }
}
LightShader.defaultVertexSrc = vert;

export { LightShader };
//# sourceMappingURL=LightShader.mjs.map
